#include <bits/stdc++.h>
using namespace std;

void solve()
{
    cout << fixed << setprecision(10) << 2 * (sqrt(5.5 * 5.5 * 2) + sqrt(4.5 * 4.5 * 2));
    return;
}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    solve();
    return 0;
}